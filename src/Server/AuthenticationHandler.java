package Server;

import SocketCommunication.Credentials;

public class AuthenticationHandler {

    public static boolean Authenticate (Credentials credentials){

        // If the user does not exist, return false if the user does not exist.

        // If the user does exist ...

        // Concatenate the password and the salt

        // Hash the result

        // If the result of the password + salt hash matches the content of the database, return true


        return false; // Assume authentication failed if no success case reached
    }


}
